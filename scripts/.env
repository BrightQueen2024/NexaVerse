# NEXAVERSE PRODUCTION CONFIGURATION
# Enforces Zero-Junk / Active-Only Architecture

# Network Gateway
PORT=5000
JWT_SECRET=nexa_core_secure_vault_2026_prod

# Database Path (Relative to scripts/)
DB_PATH=./nexa_vault.db

# Live Service Endpoints
API_BASE_URL=http://localhost:5000/api/v1
GOLD_SPOT_API=https://api.metalpriceapi.com/v1/latest

# System Performance
DEBUG_MODE=false
LOG_LEVEL=info
